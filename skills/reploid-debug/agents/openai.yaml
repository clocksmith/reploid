interface:
  display_name: "Reploid Debug"
  short_description: "Diagnose a named Reploid failure"
  default_prompt: "Use $reploid-debug to reproduce and diagnose this failure in Reploid; repair it only if requested."

Reploid, Simulatte, and Mandate are deployed, but the complete product finish line remains open. Retained [evidence](../../artifacts/product-closure-2026-09-08/README.md) separates implementation, physical execution, semantic review, and independent operation. The archive preserves failures, exact outputs, source identities, and subsequent corrections.

Reploid production runs d1ba8ddc. Cancellation now observes the requester’s actual execution-start event and prevents automatic local recomputation after a contributor starts. Explicit recovery remains available. The deployed release sequence passed inference, requester reload recovery, explicit retry, cancellation, stale-result rejection, manifest corruption rejection, cached-shard recovery, and queued-provider continuity. Its ninth lane failed downloading a GCS shard with Chromium QUIC retransmission exhaustion. Consequently, the overall release record remains failed. A separate recorder correction preserves failed lanes even when their expected success receipt never existed; it cannot turn missing evidence into a pass.

Incoming Reploid changes require a generation API absent from public Doppler 0.6.0. They remain outside production. Doppler’s incoming source now has synchronized runtime closure, export declarations, API inventory, routing inventory, and measured package limits. All 1,783 locally installed files matched the candidate archive. Twenty-four Apple GPU sampling cases passed. Installed generation and signed Capsule handoff tests passed with injected execution. These checks establish scoped contracts, not model answer quality. This host currently returns HTTP 401 from npm’s publisher identity check. Restore authentication locally; publish a new immutable version only after its remaining release checks, then verify registry installation and repin Reploid.

Real Qwen execution completed both new eight-case development runs. Neither qualified. The incoming prompt met one case’s semantic requirements; a shorter experimental prompt met none and was not promoted. The previously reported seven-of-eight run was located: seven answers passed syntax inspection, but only four met the retained semantic review. Its replay answer missed reuse behavior, storage omitted location, and one conflict selected a single answer. The untouched reviewer corpus remains unopened. Further work must improve actual supported answers before blinded qualification; citation formatting alone cannot close this gate.

Simulatte production runs 67406eb2. Sun Walker now renders depth-tested shadows from three-dimensional geometry and correctly preserves eastward pedestrian turns. Existing separate sidewalk sampling remains intact. A physical pixel probe verified opposite shadow directions and nighttime disablement. Desktop and mobile journeys passed; an earlier preview frame-stall failure remains retained. Mandate production runs adc44efe, executable 0.20.2. Live desktop/mobile games, thirteen links, and private-lore exclusions passed.

Useful adapter training and promotion, controlled loading comparisons, independently operated delegation, supplier disappearance, restart/replay reproduction, human training reviews, and Mandate playtesting remain unfinished. Machine aliases and operators are still unavailable. Learned scheduling and distributed MoE remain inactive.

A successful same-machine browser test does not establish another person’s participation or an independent computer.

Component: product closure. Intent: preserved. Acceptance evidence: linked archive and reports. Boundary effects: runtime, verification, rendering, package integration, and deployments.

Reploid product closure handoff: September 7, 2026

The finish line remains a public assistant that keeps private retrieval local, borrows another operator's GPU with explicit consent, exchanges useful coding adapters, survives failures, and continues after its original supplier disappears. Prioritize product closure and physical proof.

Release: Doppler 0.6.0 is published on npm. Its clean registry installation verified all 1,779 files against the archive. Reploid pins 0.6.0. Production https://replo.id reports source 956c26a8 and Cloud Run revision reploid-pool-00082-mvj. Npm authentication is no longer blocking. ESM-2 uses the signed Capsule API, verified OPFS caching, and retained physical Apple execution. Research publication now reaches the coordinator and survives public readback. These observations do not establish independent operation.

Implementation is pushed as c0d69b61. It adds bounded idle session retention, explicit memory reservations, eviction, trust revalidation, and deterministic cleanup. Reservations estimate memory from artifact sizes and configured overhead; they are not measured GPU residency. Failed cleanup quarantines ownership instead of silently forgetting sessions. Invalid requests are rejected before loading. Periodic provider advertisements no longer overwrite active execution status.

Document sharing now exposes local-only, derived-remote, and public-remote classes. Local-only is the default. Changing classes invalidates disclosure approval; private documents and retrieval remain local. The shared answer prompt and inspector support explicit unknown statements alongside referenced facts.

Physical answer evaluation now exists in scripts/evaluate-document-answers.js. The pinned Qwen checkpoint matched retained source hashes; conversion reproduced all 54 signed Capsule dependencies. Two complete eight-case Apple GPU runs used published Doppler. Neither passed semantic qualification: observed failures include invented throughput, omitted contradictions, and inappropriate answers to unanswerable questions. Prompt changes alone have not solved factuality. Preserve both runs and review every factual sentence against its cited passage.

Validation: the full suite reported 2,450 passed, 35 skipped, and two replay-test failures. All 21 replay tests passed in isolation. Broad Verification Worker inspection reports existing forbidden browser-storage accesses in test files; changed production modules passed separately. Generated inventory, VFS, and browser manifests are rebuilt; browser qualification remains pending. Workspace pull completed after a Gamma index-write failure.

Remaining ordered work: finish release cancellation qualification; provide qualified Qwen, EmbeddingGemma, reranker, approved-adapter, and ESM presets; train and objectively qualify a useful coding adapter; demonstrate adapter-only transfer, interruption recovery, durable replay, and reversed ownership. Prove borrowed computation with private retrieval on two independently operated computers. Measure actual loading improvements. Diagnose AMD Chromium startup failures without retries. Reproduce supplier loss and repeat use with unrelated operators. Preserve the four-machine acquisition gate. Keep learned scheduling and distributed MoE inactive until prerequisites pass.

Evidence and logs: /tmp/reploid-useful-specialization-20260907 and /tmp/reploid-esm2-capsule-rsgCmW. Independent machines remain unavailable. Firebase's temporary signup quota was restored.

Component: Reploid execution and assistant closure. Intent: preserved. Acceptance evidence: named logs and receipts. Boundary effects: Doppler sessions, disclosure, evaluation, qualification.

{
  "id": "mamba",
  "name": "Mamba (SSM)",
  "modelType": "mamba",

  "inference": {
    "normalization": {
      "rmsNormWeightOffset": false,
      "rmsNormEps": 1e-5
    },
    "layerPattern": {
      "type": "custom"
    }
  },

  "tokenizer": {
    "addBosToken": true
  },

  "tensorPatterns": {
    "embedding": [
      "backbone.embedding.weight",
      "embeddings.weight"
    ],
    "lmHead": [
      "lm_head.weight"
    ]
  },

  "detection": {
    "architecturePatterns": ["mamba", "MambaForCausalLM"],
    "modelTypePatterns": ["mamba"]
  }
}

{
  "name": "decode-throughput",
  "description": "Decode throughput profile: larger GPU batches and deferred readback to reduce submit/map overhead.",
  "extends": "default",
  "runtime": {
    "inference": {
      "batching": {
        "batchSize": 8,
        "stopCheckMode": "batch",
        "readbackInterval": 8,
        "ringTokens": 8,
        "ringStop": 8,
        "ringStaging": 8
      }
    }
  }
}

{
  "name": "debug-logits2",
  "description": "Trace logits only with a top-k probe (single token).",
  "extends": "modes/debug",
  "runtime": {
    "inference": {
      "prompt": "The sky is",
      "batching": { "maxTokens": 1 },
      "sampling": { "temperature": 0 }
    },
    "shared": {
      "debug": {
        "logLevel": { "defaultLogLevel": "debug" },
        "trace": {
          "enabled": true,
          "categories": ["logits"]
        },
        "probes": [
          { "id": "topk", "stage": "logits_final", "tokens": [-1], "dims": [138, 496, 0, 1, 2, 3] }
        ]
      }
    }
  }
}

export function initPriorities(count: number, mode: string, seed: number, scale: number): Float32Array;
export function perturbPriorities(base: Float32Array, rng: () => number, count: number, scale: number): Float32Array;

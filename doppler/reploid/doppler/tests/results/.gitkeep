# Benchmark Results Directory

This directory stores benchmark result JSON files.

Results can be:
- Saved automatically via `saveResult()` to IndexedDB
- Exported via `downloadAsJSON()` to this directory
- Committed for regression tracking

File naming convention: `{suite}_{model}_{timestamp}.json`

See `docs/spec/BENCHMARK_HARNESS.md` for schema details.
